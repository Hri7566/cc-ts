// await = await,
// async = async,
// hatsune = hatsune,
// miku = miku,
// awaitSafe = awaitSafe,
// throw = throw,
// exception = exception,
// Exception = Exception,
// logger = setLogger,
// version = version

declare function miku(func: (resolve: (...args: any) => any, reject: (...args: any) => any) => any): any;
